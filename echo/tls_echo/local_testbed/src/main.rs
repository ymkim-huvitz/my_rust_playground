fn main() {
    println!("how to run this example:");
    println!("cargo run --bin svr : to start the echo server");
    println!("cargo run --bin reverse_proxy : to start the TLS reverse proxy");
    println!("cargo run --bin proxy : to start the TLS proxy");
    println!("cargo run --bin cli : to start the echo client");
}
